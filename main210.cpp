#include <iostream>
using namespace std;

int main(void)
{
    int N, Y;
    bool flag = false;
    for (int i = 0; i <= N; ++i)
    {
        for (int j = 0; j <= N; ++j)
        {
            for (int k = 0; k <= N; ++i)
            {
                if (i + j + k == N)
                {
                    if (10000 *)
                }
            }
        }
    }
}
