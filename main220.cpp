#include <iostream>
#include <vector>
using namespace std;

int main(void)
{
    int H, W;
    cin >> H >> W;

    if (H != 1)
        return 0;

    vector<int> A(W + 1);
    for (int i = 1; i < W + 1; ++i)
        cin >> A[i];

    for (int i = 1; i < W + 1; ++i)
    {
    }
}
